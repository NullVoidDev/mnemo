// Command mnemo is the Mnemo CLI and daemon entry point.
//
// Mnemo is a portable, open, shareable memory layer for small local LLMs.
// See https://github.com/NullVoidDev/mnemo for details.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "mnemo: not implemented yet — see docs/ for the design work in progress")
	os.Exit(1)
}
